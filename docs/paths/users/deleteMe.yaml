/users/deleteMe:
  delete:
    tags: [Users]
    summary: Deactivate current user
    description: Soft-deletes the authenticated user by setting active=false.
    responses:
      '204':
        description: User deactivated
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserDeleteResponse'
